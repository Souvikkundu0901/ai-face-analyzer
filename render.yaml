services:
  - type: web
    name: ai-face-analyzer
    env: python
    buildCommand: "pip install -r requirements.txt uvicorn[standard]"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
    plan: free
    autoDeploy: true
